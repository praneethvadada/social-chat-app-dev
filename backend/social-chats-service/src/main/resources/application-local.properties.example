# Copy this file to application-local.properties (same directory) and fill
# in real values. application-local.properties is gitignored — never commit
# it. Spring Boot loads it automatically (see spring.config.import in
# application.properties); anything set here overrides application.properties.

agora.app-id=
agora.app-cert=
